%h1= @post.title


%em
  von 
  =@post.user.name

%p
  =@post.body


- if @post.photo.attached? 
  
  %br
  = image_tag(@post.photo.variant(resize: 200), width: 200) 
  %br
  %br

%br
%br
%hr
%br

= link_to t('content.edit'), edit_post_path(@post)
\|
= link_to t('navigation.back'), posts_path
