- title @users.length.to_s + " Benutzer"

%table
  %thead
    %tr
      %th ID
      %th Name
      %th E-Mail
      %th
      %th
  %tbody
    - @users.each do |user|
      %tr
        %td= user.id
        %td= link_to user.name, user
        %td= user.email
        %td= link_to "bearbeiten", edit_user_path(user)
        %td= link_to "löschen", user, method: :delete, data: { confirm: "Sind Sie sicher?" }
